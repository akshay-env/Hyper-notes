# ── Dependencies ────────────────────────────────────────────────────────────
node_modules/

# ── Build output ────────────────────────────────────────────────────────────
dist/
src-tauri/target/
src-tauri/gen/schemas/
# AssemblyScript intermediate output. The physics module is committed in its
# EMBEDDED form (src/graph/physicsWasmBinary.ts, generated by scripts/embed-wasm.cjs),
# so the loose .wasm/.wat here are regenerable artifacts, not source.
build/

# ── Reference material, not part of the app ─────────────────────────────────
# Third-party source kept locally for reference, and a vendored SolidJS docs
# bundle. Both carry their own upstream licensing and are not this project's
# code — kept out of the repo deliberately.
Reference Code/
solidJSkills/

# ── Local tooling / logs ────────────────────────────────────────────────────
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# ── Editor / OS ─────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
